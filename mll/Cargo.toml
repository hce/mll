[package]
name = "mll"
version = "0.1.0"
edition = "2024"

[dependencies]
mllc = { path = "../mllc" }
mlua = { version = "0.10", features = ["lua54", "vendored"] }
clap = { version = "4", features = ["derive"] }
